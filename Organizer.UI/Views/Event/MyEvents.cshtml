@model IEnumerable<Organizer.Entity.Events>

@{
    ViewBag.Title = "MyEvents";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Organize Ettiğim Etkinlikler</h2>

<p>
    @Html.ActionLink("+ Yeni Etkinlik", "CreateEvent")
</p>
<table class="table">
    <tr>
        <th>
            Resim
        </th>
        <th>
            Etkinlik Adı
        </th>
        @*<th>
            Oluşturan Kişi
        </th>*@
        <th>
            Kontenjan
        </th>
        <th>
            Etkinlik Tarihi
        </th>
        @*<th>
            Kategori
        </th>*@
        <th>
            Başvuru Tarihi
        </th>
        <th>
            Açıklama
        </th>

        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Picture)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        @*<td>
            @Html.DisplayFor(modelItem => item.OwnerID)
        </td>*@
        <td>
            @Html.DisplayFor(modelItem => item.NumberOfJoined)/@Html.DisplayFor(modelItem => item.Capacity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EventDate)
        </td>
        @*<td>
            @Html.DisplayFor(modelItem => item.CategoryID)
        </td>*@
        <td>
            @Html.DisplayFor(modelItem => item.ApplicationDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>

        <td>
            @Html.ActionLink("Düzenle", "UpdateEvent", new { id = item.ID }) |
            @*@Html.ActionLink("Details", "Details", new { id=item.ID }) |*@
            @Html.ActionLink("Sil", "DeleteEvent", new { id = item.ID })
        </td>
    </tr>
}

</table>
